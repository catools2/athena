<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <artifactId>etl.k8s</artifactId>
    <packaging>jar</packaging>

    <name>Core Automation Toolset - Kubernetes ETL</name>
    <description>The database structure to hold kubernetes data for analytical purpose.</description>

    <parent>
        <groupId>org.catools</groupId>
        <artifactId>catools.hibernate.parent</artifactId>
        <version>${revision}</version>
        <relativePath>../catools.hibernate.parent/pom.xml</relativePath>
    </parent>

    <dependencies>
        <!-- ######################  CATS ######################### -->
        <dependency>
            <groupId>org.catools</groupId>
            <artifactId>common.concurrent</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>org.catools</groupId>
            <artifactId>common.k8s</artifactId>
            <version>${revision}</version>
        </dependency>
        <!-- ######################  /CATS ######################### -->
    </dependencies>
</project>
